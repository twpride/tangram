export const tL = 100;

// vertices are listed in clockwise direction
export const shapeGeoms = [
  [
    [-0.5 * tL, -0.5 * tL],
    // [0, -0.04 * tL],
    [0, 0],
    // [-0.04 * tL, 0],
    [-1 * tL, 0],
    [-1.96 * tL, 0],
    // [-2 * tL, 0],
    [-1.97 * tL, -0.03 * tL],
    [-0.03 * tL, -1.97 * tL],
    // [0, -2 * tL],
    [0, -1.96 * tL],
    [0, -1 * tL],
  ],
  [
    [0.5 * tL, -0.5 * tL],
    // [0.04 * tL, 0],
    [0, 0],
    // [0, -0.04 * tL],
    // [0, -1 * tL],
    [0, -1.96 * tL],
    // [0, -2 * tL],
    [0.03 * tL, -1.97 * tL],

    [1.97 * tL, -0.03 * tL],
    // [2 * tL, 0],
    [1.96 * tL, 0],
    // [1 * tL, 0],
  ],


  [
    [-1 * tL, 0.5 * tL],
    // [-1.03 * tL, 0],
    [-1 * tL, 0],
    // [-0.98 * tL, 0.02 * tL],

    // [-0.5 * tL, 0.5 * tL],

    [-0.03 * tL, 0.97 * tL],
    // [0, 1 * tL],
    [-0.04 * tL, 1 * tL],

    // [-0.97 * tL, 1 * tL],
    [-1 * tL, 1 * tL],
    // [-1.02 * tL, 0.98 * tL],

    [-1.5 * tL, 0.5 * tL],
    [-1.97 * tL, 0.03 * tL],
    // [-2 * tL, 0],
    [-1.96 * tL, 0],
  ],

  [
    [0.5 *tL, 0.5 * tL],
    // [0, 0.04 * tL],
    [0, 0],
    // [0.04 * tL, 0],
    // [0.96 * tL, 0],
    [1 * tL, 0],
    // [1 * tL, 0.04 * tL],
    // [1 * tL, 0.96 * tL],
    [1 * tL, 1 * tL],
    // [0.96 * tL, 1 * tL],
    // [0.04 * tL, 1 * tL],
    [0, 1 * tL],
    // [0, 0.96 * tL]
  ],
  [
    [0, 1.5 * tL],
    [0.96 * tL, 1 * tL],
    // [1 * tL, 1 * tL],
    [0.97 * tL, 1.03 * tL],
    [0.5 * tL, 1.5 * tL],
    // [0.02 * tL, 1.96 * tL],
    [0, 2 * tL],
    // [-0.02 * tL, 1.96 * tL],
    [-0.5 * tL, 1.5 * tL],

    // [-0.97 * tL, 1.03 * tL],
    [-1 * tL, 1 * tL],
    // [-0.96 * tL, 1 * tL]
  ],  
  [
    [-0.25 * tL, 0.25 * tL],
    // [-0.04 * tL, 0],
    [0, 0],
    // [0, 0.04 * tL],
    [0, 0.7 * tL],
    [0, 0.96 * tL],
    // [0, 1 * tL],
    [-0.03 * tL, 0.97 * tL],
    [-0.97 * tL, 0.03 * tL],
    // [-1 * tL, 0],
    [-0.96 * tL, 0],
    [-0.4 * tL, 0],
  ],
  [
    [1.25 * tL, 0.25 * tL],
    // [1 * tL, 0.04 * tL],
    // [1.04 * tL, 0],
    [1 * tL, 0],
    [1.5 * tL, 0],
    [1.96 * tL, 0],
    // [2 * tL, 0],
    [1.97 * tL, 0.03 * tL],
    [1.03 * tL, 0.97 * tL],
    // [1 * tL, 1 * tL]
    [1 * tL, 0.96 * tL],
    [1 * tL, 0.2 * tL],
  ]
]